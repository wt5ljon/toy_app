<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
	<strong>First Post:</strong>
	<%= @user.microposts.first.content if @user.microposts.length > 0 %>
</p>

<p>
	<strong>Number of Posts:</strong>
	<%= @user.microposts.length %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
